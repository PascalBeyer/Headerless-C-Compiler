
int arst(){
    
    int *it; 
    char *k2 = 0;
    
    
    int a1, a2, a3, a4;
    int b1, b2, b3, b4;
    int c1, c2, c3, c4;
    int d1, d2, d3, d4;
    
    (k2); // This used to report an undeclared identifier, because 'it' and 'k2' hash to the same thing and the array growing function was wrong.
    return 0;
}

int main(){}
